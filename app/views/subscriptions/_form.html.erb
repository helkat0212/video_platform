<%= form_with(model: subscription) do |form| %>
  <% if subscription.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(subscription.errors.count, "error") %> prohibited this subscription from being saved:</h2>

      <ul>
        <% subscription.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :userId, style: "display: block" %>
    <%= form.number_field :userId %>
  </div>

  <div>
    <%= form.label :channelId, style: "display: block" %>
    <%= form.number_field :channelId %>
  </div>

  <div>
    <%= form.label :startDate, style: "display: block" %>
    <%= form.date_field :startDate %>
  </div>

  <div>
    <%= form.label :endDate, style: "display: block" %>
    <%= form.date_field :endDate %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
